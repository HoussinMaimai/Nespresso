package nespresso;

import org.junit.jupiter.api.Test;
import org.openqa.selenium.chrome.ChromeDriver;

public class Nespresso {
   /* public static void main(String[] args) throws InterruptedException {

        initializer initializer = new initializer();
        ChromeDriver driver = initializer.initDriver();

        LunchWebSite lunchWebSite = new LunchWebSite(driver);
        lunchWebSite.lunch(AppConstants.APP_URL);

         /*JavascriptExecutor jsExecutor = (JavascriptExecutor) driver;
         JsExecutor jsScript = new JsExecutor(jsExecutor);
        jsScript.runJs("cmp_pv.cookie.saveConsent(true);");

        VerfierPage verfierPage = new VerfierPage(driver);
        String checkPage = "LIVRAISON GRATUITE";
        verfierPage.pageVerification(checkPage);

        Login log = new Login(driver);
        log.Log_in();
        AddCart addToCart = new AddCart(driver);
        addToCart.cliqueCafe();
     }
   @Test
   public void Test01() throws InterruptedException {
       String values=ExcelReader.getProductNameFromExcel();
       System.out.println("site"+values);
   }
    @Test
    public void TestLog() throws InterruptedException {
        initializer initializer = new initializer();
        ChromeDriver driver = initializer.initDriver();
        LunchWebSite lunchWebSite = new LunchWebSite(driver);
        lunchWebSite.lunch(ExcelReader.readSiteValueFromExcel());
        //String s=Excelreader.getProductQuantityFromExcel();
        Login log = new Login(driver);
        log.Log_In();
        driver.quit();
    }*/
}

